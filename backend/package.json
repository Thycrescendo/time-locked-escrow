{
  "name": "time-locked-escrow-backend",
  "version": "1.0.0",
  "scripts": {
    "build": "asc contracts/TimeLockedEscrow.asc -b build/TimeLockedEscrow.wasm",
    "deploy": "ts-node scripts/deploy.ts",
    "interact": "ts-node scripts/interact.ts",
    "test": "jest --detectOpenHandles"
  },
  "dependencies": {
    "@massalabs/as": "^0.1.0",
    "@massalabs/massa-web3": "^3.0.0",
    "as-types": "^0.2.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1"
  }
}